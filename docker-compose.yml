version: '3.9'

services:

  flask_app:
    build: ./api
    container_name: reposteria_flask
    restart: always
    depends_on:
      - postgres_db
    ports:
      - "5000:5000"
    networks:
      - reposteria_net

  postgres_db:
    image: postgres:15
    container_name: postgres_db
    restart: always
    environment:
      POSTGRES_DB: reposteria_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - reposteria_net

  nginx:
    image: nginx:latest
    container_name: reposteria_nginx
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf
      - ./pasteleria/dist/pasteleria:/usr/share/nginx/html
    depends_on:
      - flask_app
    networks:
      - reposteria_net

networks:
  reposteria_net:

volumes:
  postgres_data:

